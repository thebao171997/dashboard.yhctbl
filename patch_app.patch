--- src/App.tsx
+++ src/App.tsx
@@ -2,6 +2,7 @@
 import { Menu, PenSquare, LayoutDashboard, Building2, LogOut, Lock } from 'lucide-react';
 import Dashboard from './components/Dashboard';
 import DataEntry from './components/DataEntry';
 import ManageDepts from './components/ManageDepts';
 import { UserContext, useUser } from './context/UserContext';
 import clsx from 'clsx';
 
@@ -10,6 +11,7 @@
   const [password, setPassword] = useState('');
   const [loginError, setLoginError] = useState('');
   const [sidebarOpen, setSidebarOpen] = useState(true);
+  const [theme, setTheme] = useState('light');
   const isAdmin = userCtx?.user?.role === 'admin';
 
   const handleLogin = async (e: React.FormEvent) => {
@@ -27,8 +29,14 @@
     }
   };
 
+  useEffect(() => {
+    if (theme === 'dark') document.documentElement.classList.add('dark');
+    else document.documentElement.classList.remove('dark');
+  }, [theme]);
+
   return (
-    <div className="h-screen bg-slate-100 flex flex-col font-sans text-slate-900 overflow-hidden">
+    <div className="h-screen bg-slate-100 dark:bg-slate-900 flex flex-col font-sans text-slate-900 dark:text-slate-100 overflow-hidden transition-colors">
       {/* Top Header */}
       <header className="h-16 bg-slate-900 border-b border-slate-800 flex items-center justify-between px-6 shrink-0 text-white z-20 shadow-md">
         <div className="flex items-center gap-4">
@@ -43,11 +51,21 @@
             <p className="text-xs text-slate-400 mt-0.5">Hệ thống quản lý số liệu</p>
           </div>
         </div>
-        <h2 className="hidden md:block text-sm font-medium text-slate-400 bg-slate-800/50 px-4 py-1.5 rounded-full">
-          {currentTab === 'dashboard' && 'Tổng quan số liệu'}
-          {currentTab === 'entry' && 'Nhập số liệu báo cáo'}
-          {currentTab === 'depts' && 'Quản lý danh mục khoa'}
-        </h2>
+        <div className="flex items-center gap-4">
+          <h2 className="hidden md:block text-sm font-medium text-slate-400 bg-slate-800/50 px-4 py-1.5 rounded-full">
+            {currentTab === 'dashboard' && 'Tổng quan số liệu'}
+            {currentTab === 'entry' && 'Nhập số liệu báo cáo'}
+            {currentTab === 'depts' && 'Quản lý danh mục khoa'}
+          </h2>
+          <div className="flex items-center bg-slate-800 p-1 rounded-full border border-slate-700">
+            <button onClick={() => setTheme('light')} className={`w-8 h-8 rounded-full flex items-center justify-center transition-all ${theme === 'light' ? 'bg-white shadow-md' : 'hover:bg-slate-700'}`}>
+              <div className={`w-4 h-4 rounded-full ${theme === 'light' ? 'bg-slate-800' : 'bg-slate-400 border border-slate-500'}`} />
+            </button>
+            <button onClick={() => setTheme('dark')} className={`w-8 h-8 rounded-full flex items-center justify-center transition-all ${theme === 'dark' ? 'bg-black shadow-md' : 'hover:bg-slate-700'}`}>
+              <div className={`w-4 h-4 rounded-full ${theme === 'dark' ? 'bg-slate-300' : 'bg-slate-500 border border-slate-600'}`} />
+            </button>
+          </div>
+        </div>
       </header>
 
       <div className="flex-1 flex overflow-hidden">
